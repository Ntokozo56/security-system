# security-system
This Java project features a Security System using a Client-Server model. A JavaFX Home client triggers alarms via a "132" code sequence. The Control Room Server manages dispatching through GUI prompts, while a Swing-based Guard client handles real-time response phases ("On my way", "Arrived", "Save") via periodic socket polling.
